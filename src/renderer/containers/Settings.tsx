import { Grid } from 'semantic-ui-react';

export default function Settings() {
  return (
    <Grid container columns={1}>
      <Grid.Row>
        <h1>User Profile</h1>
      </Grid.Row>
      <Grid.Row />
      <Grid.Row />
      <Grid.Row />
    </Grid>
  );
}
